---
team: 'release'
apt_host: saturn.delivery.puppetlabs.net
apt_signing_server: saturn.delivery.puppetlabs.net
yum_host: saturn.delivery.puppetlabs.net
yum_staging_server: saturn.delivery.puppetlabs.net
svr4_host: saturn.delivery.puppetlabs.net
p5p_host: saturn.delivery.puppetlabs.net
ips_host: ladybug.delivery.puppetlabs.net
nuget_host: nexus.delivery.puppetlabs.net:8081
nuget_repo_path: /content/repositories/nuget-pl-build-tools
svr4_path: /opt/build-tools/solaris
p5p_path: /opt/build-tools/solaris
ips_path: /export/repo-pl-build-tools
apt_repo_path: /opt/tools/freight.build-tools/apt
yum_repo_path: /opt/build-tools/yum
ips_repo: pl-build-tools
apt_repo_name: ''
yum_repo_name: ''
apt_repo_command: export GPG_TTY=$(tty); sudo -E freight-cache -c /etc/freight.conf.d/pl-build-tools-repo.conf
yum_repo_command: |
  for repodir in $(find "__REPO_PATH__" -mindepth 2 -name "*.rpm" | xargs -I {} dirname {} | uniq) ; do
    [ -d "${repodir}" ] || continue;
    echo "Generating repodata for ${repodir}..."
    sudo createrepo --checksum=sha --database --update "${repodir}";
    echo "Finished generating repodata for ${repodir}..."
  done;
osx_signing_cert: "Developer ID Installer: PUPPET LABS, INC. (VKGLGN2B6Y)"
osx_signing_keychain: "/Users/jenkins/Library/Keychains/signing.keychain"
osx_signing_server: osx-signer.delivery.puppetlabs.net
ips_signing_server: rama.delivery.puppetlabs.net
